# 手繪設計稿原始來源

本資料夾保存 0.9.1 ORIGIN 敘事使用的原始照片與草圖：

```text
source-materials/concepts/hand-sketches/
```

## 保存原則

- 原始檔案，不要先壓縮、裁切、套濾鏡或覆蓋舊檔。
- 手機／相機原有的最高解析度。
- 如果有正反面、不同階段或局部細節，請各自保存為獨立檔案。

2026-07-25 收件的 12 個檔案已依來源收件規範完成尺寸、雜湊與拍攝方向檢查，穩定編號為：

```text
SRC-CONCEPT-012 … SRC-CONCEPT-023
```

網站使用的方向校正與壓縮版本位於
`reference/src/views/design-concept/assets/`；本資料夾中的原始位元組未被修改。

## 0.9.1 公開選擇

- 校園場勘只在 HTML 使用 `SRC-CONCEPT-013`、`SRC-CONCEPT-016`、`SRC-CONCEPT-017`，即原「校園場勘 2、5、6」。
- 手稿在 HTML 使用 `SRC-CONCEPT-018`～`SRC-CONCEPT-022`。
- `SRC-CONCEPT-023_stair-concept-reference.png` 只保存來源，不放入 HTML。
- `SRC-CONCEPT-012`、`SRC-CONCEPT-014`、`SRC-CONCEPT-015` 只保存來源，不放入 HTML。

完整 MIME、bytes、dimensions、SHA-256、用途與公開狀態登錄於
`docs/02_SITE_AND_SOURCES.md`。
