# Release 0.3.2

- 發布日期：2026-07-17
- package version：`0.3.2`
- modelVersion：`0.3.2`
- revision：`2026-07-17`
- release commit：`release: v0.3.2`（本文件所在提交）
- Git tag：未建立；只有使用者另行明確要求時才建立 tag
- Push／部署：本 release commit 推送至 `main`，由既有 GitHub Actions／Pages workflow 建置公開 HTML；部署結果以 GitHub workflow 為準

## 完成範圍

- `TASK-015`：修正手機版操作滑桿時，控制器與平面／剖面示意圖相距過遠而無法同時觀察變化的問題。
- 920 px 以下在控制器上方提供 sticky 即時預覽；預覽停留於安全區下方，不移除頁面原有完整平面與剖面圖。
- 年份、日期、時間與 2F 水平旋轉控制自動顯示平面；鏡牆外傾角自動顯示剖面，並提供平面／剖面手動切換。
- 即時預覽複製目前 SVG 狀態並改寫內部 ID 與 marker 引用，避免與完整原圖產生重複 ID；桌面寬度不建立隱藏 SVG 副本。

## 驗證證據

- `npm.cmd run check:docs`：通過；文件連結、15 個 TASK、spec lifecycle 與 release 結構一致。
- `npm.cmd run validate:reference`：通過；model 0.3.2、29 entities、6 sheets、12 sources。
- `npm.cmd test`：通過；73 項測試包含手機 sticky 即時預覽來源回歸。
- `npm.cmd run build`：通過；產生 `dist/reference/index.html` 與 `dist/reference/solar-study/index.html`。
- Chrome 390 × 844 smoke：日期控制顯示平面、外傾角控制顯示剖面；預覽實際停在頂端 6 px，預覽與作用中控制器不重疊，頁面無水平溢出，DOM 無重複 ID。
- `git diff --check`：通過。

## 已知限制

- 本版本不改變 +9.5° 水平旋轉、+8.5° 鏡牆外傾或任何日照、PVGIS 與反射分析結論。
- `OPEN-006`、`OPEN-008`、`OPEN-009`、`OPEN-011`、`OPEN-013`、`OPEN-014` 仍依 owner 文件保持未決。
- 本回合由手機端遠端控制，以 Chrome 390 × 844 裝置尺寸驗證滑桿附近的畫面與事件切換；未在實體手機硬體上執行人工觸控驗證。
