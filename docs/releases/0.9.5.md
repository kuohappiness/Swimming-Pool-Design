# Release 0.9.5

- 日期：2026-07-26
- 版本：0.9.5
- Schema：1.4.0
- Active geometry：`GEO-0.8.2`
- 完成任務：`TASK-096`～`TASK-097`
- 發布方式：依使用者要求建立 0.9.5 工作樹；本輪未要求 commit、tag 或 push

## ORIGIN 敘事精修

- 將三張場勘照片改為單欄，依原始入口寬度、泳池本體寬度、老樹與新入口邊界排列。
- 將手稿改為日照方位、光線反射、1F 俯視、剖面、早期立體圖的單欄順序。
- 同步核准標題、說明、替代文字與瀏覽器驗收。

## 回退邊界

- 專案由 0.9.4 底座重建，只保留 ORIGIN 精修。
- active geometry、Solar Study、solar inputHash、V067 圖面與 3D／漫遊行為維持 0.9.4 基準。
- 工作任務序列截止於 `TASK-097`。
- 回退前內容已保存於本機 Git stash，不存在於 0.9.5 工作樹。

## 驗證

- `npm.cmd run build`：通過；84 份 Markdown、97 個任務、reference model、93 項測試、TypeScript 與 production bundle 成功。
- `npm.cmd run test:e2e`：通過；四 view × 五 viewport，以及 Viewer、Solar Study、atlas 的桌機、手機與 WebGL fallback 成功。
- `npm.cmd run capture:copy`：通過；四頁可見文字快照更新為 0.9.5。
- `git diff --check`：通過。
