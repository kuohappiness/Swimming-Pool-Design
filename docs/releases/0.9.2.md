# Release 0.9.2

- 日期：2026-07-26
- 版本：0.9.2
- Schema：1.4.0
- Active geometry：`GEO-0.8.2`
- 任務：`TASK-088`～`TASK-091`
- 決策：`DEC-136`～`DEC-138`
- 發布方式：單一 release commit 推送至 `main`；不建立 tag

## 四分頁文字與版面細修

- 統一設計理念頁的章節標題、註解、斷行、欄寬、圖文對齊與閱讀節奏。
- 重整日照研究導言及「日照研究模型」層級，移除重複摘要並縮短無內容留白。
- 精簡圖面設計頁的導言文字與空白區，V067 圖面內容及雜湊保持不變。
- 將 3D 展示整理為聚焦介面：狀態改用顏色圓點，攝影機按鈕縮短，第一人稱入口改為「實境漫遊」並移至導言區；場景敘事及右側構件資訊只暫時隱藏，原始資料與 runtime 均保留。
- 頁首採用使用者提供的透明校徽來源，並登錄原始檔與衍生檔尺寸、雜湊。

## v1.0 文字備份

新增 `scripts/capture-public-copy.mjs` 與 `npm run capture:copy`，透過 Chromium 讀取四個公開分頁實際渲染後的 `document.body.innerText`，輸出 `docs/public/website-copy-v1.0.md`。快照保存路徑、可見斷行及逐頁 SHA-256，供後續文字修訂及改版比對；隱藏內容不列入快照，但仍保留於程式來源。

## 不變邊界

- active geometry 維持 `GEO-0.8.2`。
- 日照 inputHash、工作支點與 v0.6.7 分析結果不變。
- V067 SVG／PNG 完稿內容不變。
- 3D 幾何、相機、圖層、選取、碰撞、移動、游泳、safe spawn 與畫質行為不變。

## 驗證

- `npm run build`：通過；82 份 Markdown、91 項任務、50 個 entity、5 張 sheet、16 個來源、93 項單元測試與 TypeScript 檢查全數通過。
- `npm run test:public`：通過；4 個公開分頁 × 5 種 viewport，涵蓋 routing、accessibility、3D runtime isolation 與 reduced motion。
- `npm run test:e2e`：通過；Viewer、solar-study 與 atlas 的 desktop、mobile、WebGL fallback、圖層、視角、選取、實境漫遊及游泳流程均通過。
- `npm run verify:v091-baseline`：通過；8 個圖面檔、solar 與 geometry 的 0.9.1 核准敘事邊界未漂移。
- `npm run capture:copy`：通過；正式建置的四個公開分頁已寫入 `docs/public/website-copy-v1.0.md` 並保存逐頁 SHA-256。
- `git diff --check`：通過。

驗證期間自我修復三項過期測試斷言（章節名稱／獨立標題、移至導言區的實境漫遊入口、暫隱藏但保留的 3D 敘事與構件介面），並修正導言殘留的「向水」為「向雨」；重新執行相關檢查後全部通過。
