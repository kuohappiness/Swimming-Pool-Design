# Release 0.9.4

- 日期：2026-07-26
- 版本：0.9.4
- Schema：1.4.0
- Active geometry：`GEO-0.8.2`
- 完成任務：`TASK-094`～`TASK-095`
- 主要決策：`DEC-140`
- 發布方式：單一 release commit 直接 push `main`，不建立 tag

## 理念文案與用詞精修

- 依核准文字精簡「向光、向雨、向人、向時間」四章及設計細節，讓公開成果展敘事更直接。
- 將面向使用者的「服務量體」統一稱為「服務中心」，同步來源描述、Viewer 場景、替代文字與日照示意。
- 為「向雨、向人、向時間」引言建立一致的左側線條、內文字體與閱讀寬度。

## 不變邊界

- active geometry 維持 `GEO-0.8.2`，不建立 `GEO-0.9.4`。
- solar inputHash、v0.6.7 研究基線與互動不變。
- V067 SVG／PNG 本體不變。
- 3D Viewer 幾何、相機、圖層、選取、碰撞、移動、游泳與漫遊行為不變。

## 驗證

- `npm run build`：通過。
- `npm run test:public`：通過。
- `npm run test:e2e`：通過。
- `npm run verify:v091-baseline`：通過。
- `git diff --check`：通過。
